upstream admin-backend {
	keepalive 512;
	server admin-backend:8000;
}

server {
	listen 80;
	root /admin-frontend-dist;

	location /api {
		include public/proxies/websocket.conf;
		proxy_pass http://admin-backend;
	}

	location /files {
		alias /files;
		expires 7d;
	}

	location / {
		expires 7d;
		include public/headers/default.conf;
		try_files $uri /index.html;
	}
}
